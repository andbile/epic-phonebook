To config file config_server.js use this manual.

module.exports = {
    server_port: 3000,
    server_enable: true/false, // true - site availability, false - if site maintenance

    // example configuration to connecting BD using node-postgres
    bd_host: '127.0.0.1',
    bd_port: '5432',
    bd_name: 'name',
    bd_user: 'user',
    bd_password: 'password'
}